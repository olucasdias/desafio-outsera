import { Before } from '@cucumber/cucumber';
import { login } from '../support/commands';

// Before({ tags: '@purchase' }, () => {
//   cy.fixture('user').then((user) => {
//     login(user);
//   });
// });
